---
title: T32 API
description: T32 설정 관리 및 Dump 실행 REST API 레퍼런스
---

## T32 Config CRUD

Base path: `/api/t32`

| Method | Path | 설명 |
|--------|------|------|
| GET | `/configs` | 전체 T32 설정 목록 (DTO, 서버 이름 해석 포함) |
| POST | `/configs` | T32 설정 추가 |
| PUT | `/configs/{id}` | T32 설정 수정 |
| DELETE | `/configs/{id}` | T32 설정 삭제 (cascade로 매핑도 삭제) |

### 요청 본문 (POST/PUT)

```json
{
  "serverGroupId": 1,
  "jtagServerId": 5,
  "jtagUsername": "",
  "jtagPassword": "",
  "t32PcId": 6,
  "t32PcUsername": "admin",
  "t32PcPassword": "secret",
  "jtagCommand": "cd T32linker && echo yes | python ./tmux_lib.py -n 1 -t {tentacle_num} -s {slot}",
  "jtagSuccessPattern": "Measured Voltage.*Register Value",
  "t32PortCheckCommand": "t32rem.exe localhost port=20000 system.mode.attach",
  "dumpCommand": "cmd /c C:\\T32\\dump.bat {tentacle} {slot}",
  "description": "Lab A T32 설정",
  "enabled": true,
  "assignedServerIds": [3, 4, 5]
}
```

### 응답 (T32ConfigDto)

```json
{
  "id": 1,
  "serverGroupId": 1,
  "serverGroupName": "Lab A",
  "jtagServerId": 5,
  "jtagServerName": "TMUX-01",
  "jtagServerIp": "192.168.1.50",
  "jtagUsername": "",
  "jtagHasCustomAccount": false,
  "t32PcId": 6,
  "t32PcName": "WIN-T32-01",
  "t32PcIp": "192.168.1.60",
  "t32PcUsername": "admin",
  "t32PcHasCustomAccount": true,
  "jtagCommand": "cd T32linker && echo yes | python ...",
  "jtagSuccessPattern": "Measured Voltage.*Register Value",
  "t32PortCheckCommand": "t32rem.exe ...",
  "dumpCommand": "cmd /c ...",
  "description": "Lab A T32 설정",
  "enabled": true,
  "assignedServers": [
    {"id": 3, "name": "T8", "ip": "172.20.0.18"},
    {"id": 4, "name": "T9", "ip": "172.20.0.19"},
    {"id": 5, "name": "T10", "ip": "172.20.0.20"}
  ]
}
```

---

## T32 Dump 실행

Base path: `/api/t32/dump`

### Config 확인

```
GET /check?serverId={id}
```

해당 서버에 T32 설정이 있는지 확인합니다. 컨텍스트 메뉴 활성화 판단용.

```json
{ "available": true, "configId": 1 }
```

### Dump 실행 (SSE)

```
POST /execute
Content-Type: application/json
→ text/event-stream
```

```json
{
  "serverId": 5,
  "tentacleName": "T10",
  "slotNumber": 1
}
```

SSE 이벤트 스트림으로 4단계 실행 진행 상황을 반환합니다.

---

## TC Pre-Command API

Base path: `/api/pre-commands`

### TC별 조회

```
GET /tc?source={source}&slotIndex={slotIndex}
```

```json
[
  { "tcId": 34, "preCommandId": 1, "preCommandName": "Init UFS" },
  { "tcId": 38, "preCommandId": 2, "preCommandName": "Hang for T32" }
]
```

### TC별 등록

```
POST /tc/assign
```

```json
{
  "preCommandId": 1,
  "source": "compatibility",
  "slotIndex": 3,
  "tcId": 34
}
```

### TC별 해제

```
POST /tc/unassign
```

```json
{
  "source": "compatibility",
  "slotIndex": 3,
  "tcId": 34
}
```

### 동작 규칙

- TC에 Pre-Command가 있으면 → 슬롯 Pre-Command 무시 (TC 우선)
- 슬롯 clear 시 → 해당 슬롯의 모든 TC Pre-Command 자동 삭제
- TC 삭제 시 → 해당 TC의 Pre-Command 자동 삭제
